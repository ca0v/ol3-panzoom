import { describe, it, should } from "ol3-fun/tests/base";
import * as PanZoom from "../ol3-panzoom/ol3-panzoom";

// no tests
describe("ol3-panzoom", () => {
	it("ol3-panzoom", () => {
		should(!!PanZoom, "PanZoom exists");
	});
});
